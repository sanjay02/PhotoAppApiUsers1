spring:
  application:
    name: users-ws

server:
  port: 8888


eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://127.0.0.1:8010/eureka/
  instance:
    hostname: localhost
